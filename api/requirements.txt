# SLIM serving image -- deliberately no TensorFlow.
#
# The whole backbone/head architecture exists so this list stays short enough
# to run inside a 512 MB container. Adding tensorflow here would defeat it.

fastapi==0.140.13
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# Inference: the frozen backbone runs on ONNX Runtime, the head on scikit-learn.
onnxruntime==1.20.1
scikit-learn==1.6.0
joblib==1.4.2
numpy==2.0.2
pillow==11.0.0

# NOT installed on purpose: opencv-python-headless (~137 MB with its bundled
# libs). src/preprocessing imports cv2 lazily, only inside colour_statistics
# and texture_features, and nothing on the request path calls them -- the API
# uses load_image, preprocess_for_backbone, split_class_name, is_healthy and
# list_images, all pure NumPy/Pillow. tests/test_serving_deps.py asserts this
# so the dependency cannot creep back in unnoticed.
