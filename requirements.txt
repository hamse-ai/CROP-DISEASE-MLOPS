# Full development / notebook environment.
#
# This is the HEAVY environment: it trains models and exports artifacts.
# The deployed containers use the much slimmer api/requirements.txt and
# ui/requirements.txt instead -- neither installs TensorFlow.
#
#   conda create -y -n cropml python=3.11 && conda activate cropml
#   pip install -r requirements.txt

# --- Deep learning (training + artifact export only) ---
tensorflow==2.19.*
numpy<2.2

# --- Classical ML: the deployed classifier head lives here ---
scikit-learn>=1.4
joblib>=1.3

# --- Data handling and imaging ---
pandas>=2.2
pillow>=10.0
opencv-python-headless>=4.9

# --- Visualisation (EDA figures consumed by the UI) ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# --- Export + inference runtime (must match api/requirements.txt) ---
onnx>=1.16
onnxruntime>=1.18
tf2onnx>=1.16

# --- Serving stack (mirrored, slimmer, in api/ and ui/) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
streamlit>=1.33
requests>=2.31

# --- Load testing ---
locust>=2.24

# --- Notebook ---
jupyter>=1.0
notebook>=7.0
nbformat>=5.10
tqdm>=4.66

# --- Tests ---
pytest>=8.0
httpx>=0.27
